name: Main
on:
  push:
    branches:
      - main

jobs:
  test:
    name: Test
    uses: jontze/.workflows/.github/workflows/rust_test.yaml@main
    with:
      build: true
      unit-tests: true

  push-image:
    needs:
      - test
    name: Publish Image
    uses: jontze/.workflows/.github/workflows/image.yaml@main
    with:
      image_name: "wasm-function-runtime"
      push: true
